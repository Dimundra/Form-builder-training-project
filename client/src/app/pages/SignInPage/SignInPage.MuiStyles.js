const styles = (theme) => ({
  mainGridContainer: {
    height: 'calc(100vh - 64px)',
  },
  title: {
    fontWeight: 'bolder',
    paddingBottom: '15px',
  },
  TextFieldStyles: {
    width: '270px',
  },
});

export { styles };
